using System;
using System.Collections.Generic;
using System.Linq;

namespace Ttest
{
    class Program
    {
        static void Main(string[] args)
        {
            var a = Enumerable.Range(1, 16).ToArray();
            var b = Enumerable.Range(0, 16).ToArray();
            Console.WriteLine();
        }
    }
}
